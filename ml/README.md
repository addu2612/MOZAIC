# MOZAIC ML / Clustering

This folder contains ML-related utilities and clustering pipelines used by MOZAIC.

## Contents

- `clustering/`:
  - `clustering_common/`
  - `clustering_grafana/`
  - `clustering_k8s/`

## Reproducibility notes

- This snapshot **does not run training** and does not generate any results.
- Output folders are intentionally excluded from the repo (`output/`, `output_sampled/`, `outputs/`).

## Dependencies

There is no single pinned environment here yet. See `ml/requirements.txt` for a starting point and pin versions as part of the next cleanup step.
